
# Core data science
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
pyarrow>=14.0.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# Machine learning
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0
# shap intentionally omitted: the recorded champion is a Pipeline, which the
# explainer guard refuses before any TreeExplainer call, so shap is unused at
# runtime. Installing it would add numba/llvmlite to the deployment build for
# a code path that is never taken. Explainability reports UNAVAILABLE, which
# is the state the test suite actually exercises.

# Dashboard
streamlit>=1.28.0

# Notebooks
notebook>=7.0.0
ipykernel>=6.25.0
ipywidgets>=8.1.0

# HTTP / data
requests>=2.31.0
python-dateutil>=2.8.2
tqdm>=4.66.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Utilities
python-dotenv>=1.0.0
